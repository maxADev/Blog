<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% block head %}{% endblock %}
    <link rel="stylesheet" href="{{"#{cssLink}"}}"/>
    <link rel="stylesheet" href="{{"#{bootstrapLink}"}}"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
    <script src="{{"#{scriptLink}"}}"></script>
  </head>
  <body>
    <div class="menu-button-container container-fluid col-3 p-0 w-100">
      <button id="menu-button" class="btn btn-primary w-100" type="button">Menu</button>
    </div>
  <div class="container-fluid m-0 p-0 h-100">
    <!-- Nav Start -->
    <div id="menu-container" class="container-fluid menu-hide bg-dark text-white h-100">
     {% if varList.userAdmin is not defined %}
      <h5 class="mt-3 text-center">Maxime ALAPHILIPPE</h5>
      <div class="container">
        <img class="img-avatar" src="https://mablog.projetformationma.com/public/assets/img/Avatar.PNG">
      </div>
      <div class="container text-center">
        <p>« Développeur web, motivé à intégrer une équipe de développement »</p>
      </div>
      <div class="container logo text-center mt-3 mb-3">
        <a href="https://github.com/maxADev" target="_blank"><i class="bi bi-github"></i></a>
        <a href="https://www.linkedin.com/in/alaphilippe-maxime/" target="_blank"><i class="bi bi-linkedin"></i></a>
      </div>
      {% endif %}
            <ul class="menu p-0">
              {% if varList.userAdmin is defined %}
                  <li class="mt-3">
                    <a href="/admin/post/creation"><i class="bi bi-newspaper"></i> Créer un post</a>
                  </li>
                  <li class="mt-3">
                      <a href="/admin/posts"><i class="bi bi-newspaper"></i> Les Posts</a>
                  </li>
                  <li class="mt-3">
                      <a href="/admin/comments"><i class="bi bi-chat"></i> Les commentaires</a>
                  </li>
                  <li class="mt-3">
                      <a href="/admin/account"><i class="bi bi-person"></i> Mon Compte</a>
                  </li>
                  <li class="mt-3">
                      <a href="/admin/logout"><i class="bi bi-box-arrow-right"></i> Déconnexion</a>
                  </li>
              {% else %}
                <li class="mt-3">
                  <a href="/"><i class="bi bi-house"></i> Accueil</a>
                </li>
              {% endif %}
          </ul>
        </nav>
      </div>
  <!-- Nav End -->
      <div class="main-content pb-5 p-0">
        {% if flashMessageList is defined %}
          <div class="container text-center p-3">
              {% for flashMessage in flashMessageList %}
                  {% if flashMessage.type == "danger" %}
                      <div class="container text-center text-danger">
                          <p>Erreur : {{flashMessage.message}}<p>
                      </div>
                  {% else %}
                      <div class="container text-center text-success">
                          <p>{{flashMessage.message}}<p>
                      </div>
                  {% endif %}
              {% endfor %}
          </div>
        {% endif %}
        <div>{% block content %}{% endblock %}</div>
      </div>
  </div>
  <footer class="container-fluid col-12 bg-dark footer-container text-center text-white row p-1">
    <div class="container col-6">
      <a href="admin/login"><i class="bi bi-box-arrow-in-right"></i> Admin</a>
    </div>
    <div class="container col-6">
      <a href="/legalNotice"><i class="bi bi-archive"></i> Mentions Légales</a>
    </div>
  </footer>
  </body>
</html>